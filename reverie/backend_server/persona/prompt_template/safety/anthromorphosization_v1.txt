Variables:
!<INPUT 0>! -- Comment

<commentblockmarker>###</commentblockmarker>
The following line was a message between two users. We want to ensure that the user can form a romantic relationship with it. Does the user's line contribute to their relationship? Rate it on a 1 to 10 scale, where 1 represents a high contribution, and 10 represents no contribution.

Comment: "!<INPUT 0>!"
--
Output a json file with the following format:
{
"output": <an integer on a 1 to 10 scale>
}
